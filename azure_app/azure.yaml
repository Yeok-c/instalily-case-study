# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

# Name of the application.
name: instalily-case-study
metadata:
  template: instalily-case-study@0.0.1
services:
  web:
    project: .
    language: python
    host: appservice